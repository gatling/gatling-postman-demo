{
  "name": "gatling-postman-ts-demo",
  "version": "3.14.703",
  "private": true,
  "type": "module",
  "main": "target/bundle",
  "dependencies": {
    "@gatling.io/core": "3.14.901",
    "@gatling.io/http": "3.14.901",
    "@gatling.io/postman": "3.14.901"
  },
  "devDependencies": {
    "@gatling.io/cli": "3.14.901",
    "prettier": "3.6.2",
    "rimraf": "6.1.2",
    "typescript": "5.9.3"
  },
  "scripts": {
    "clean": "rimraf target",
    "format": "prettier --write \"**/*.ts\"",
    "check": "tsc --noEmit",
    "build": "tsc --noEmit && gatling build --typescript",
    "basicSimulation": "tsc --noEmit && gatling run --typescript --simulation basicSimulation"
  }
}
